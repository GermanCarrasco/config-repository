server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/transaction_db
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO